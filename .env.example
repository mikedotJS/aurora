# Local dev secrets — copy to `.env` (gitignored) and fill in.
# In debug builds `tauri dev` reads the Anthropic key from here instead of the
# macOS Keychain, avoiding the repeated keychain-access prompt.
ANTHROPIC_API_KEY=sk-ant-...
